# Optional. Lets the /projects page and homepage pull your *actual* GitHub
# pinned repos at build time (via GraphQL — the only API that exposes pins).
# No scopes needed, it's just used to identify the caller for a public read.
# Without this, the site falls back to a hardcoded pin list in src/lib/github.ts.
GITHUB_TOKEN=
